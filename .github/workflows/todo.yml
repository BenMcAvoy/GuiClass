name: todo
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Download repo
      uses: actions/checkout@v4

    - name: Run Issue Bot
      uses: derjuulsn/todo-issue@main
      with:
        excludePattern: '^(include/)'
